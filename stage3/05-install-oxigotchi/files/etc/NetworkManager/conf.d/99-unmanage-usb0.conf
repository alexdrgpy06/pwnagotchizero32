# usb0 is configured by usb-gadget.service (static 10.0.0.2 + dnsmasq).
# Keep NetworkManager from touching it so the two don't fight over the address.
[keyfile]
unmanaged-devices=interface-name:usb0

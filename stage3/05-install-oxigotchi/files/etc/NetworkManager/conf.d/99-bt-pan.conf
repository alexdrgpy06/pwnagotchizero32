# Prevent NetworkManager from managing Bluetooth PAN interfaces.
# bnep0 and bt-pan0 are managed by dhcpcd via the oxigotchi daemon.
# If NM manages these, it conflicts with dhcpcd and drops the BT tether.

[keyfile]
unmanaged-devices=interface-name:bnep*;interface-name:bt-pan*